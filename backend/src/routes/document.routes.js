const express = require('express');
const router = express.Router();

// Placeholder — expand this later for document management features
router.get('/', (req, res) => {
  res.json({ message: 'Documents endpoint — coming soon' });
});

module.exports = router;
